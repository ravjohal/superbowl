<%= form_for(super_bowl) do |f| %>
  <% if super_bowl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(super_bowl.errors.count, "error") %> prohibited this super_bowl from being saved:</h2>

      <ul>
      <% super_bowl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="field">
    <%= f.label :editing_enabled %>
    <%= f.check_box :editing_enabled %>
  </div>  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
